{% extends "base.html.twig" %}
{% block body %}
<h1 class="mt-5 ml-5">{{ program.title }} - saison {{ season.number }}</h1>
<p class="mt-3 ml-5">{{ season.description }} - {{ season.year }}</p>

{% for episode in episodes %}
    <div class="col-12 mt-2 ml-5">
        <a href="{{ path('program_episode_show', { program_id: program.id, season_id: season.id, episode_id: episode.id }) }}">Episode {{ episode.number }}</a>
    </div>  
{% endfor %}

<div class='mt-5 ml-5'>
    <a href="{{ path("program_show", { program_id: program.id }) }}">Retour au résumer</a>
</div>
{% endblock %}